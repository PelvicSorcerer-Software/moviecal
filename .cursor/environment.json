{
  "install": "set -e\n[ -f .env.local ] || cp .env.example .env.local\nnpm install\nif [ -n \"${GITHUB_PAT_OPERATOR:-}\" ]; then\n  echo \"$GITHUB_PAT_OPERATOR\" | gh auth login --with-token\nfi\n",
  "ports": [
    {
      "name": "Next.js dev server",
      "port": 3000
    }
  ]
}
