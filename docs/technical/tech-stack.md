Tech stack (suggested)

- Language: TypeScript (strict mode enabled)
- Runtime / Framework: Node.js; small server framework or Next.js for SSR if needed (no files scaffolded yet)
- Database: PostgreSQL (e.g., via Supabase) or similar relational DB
- Caching: in-memory or Redis for TMDb responses and rate-limit smoothing
- CI/CD: GitHub Actions for linting, typechecking, tests, and deploy
- Testing: Jest (unit), integration tests, and Playwright/Cypress for E2E

These are starting recommendations — choice can be refined during implementation.
